FROM alpine

COPY ./timestamp-line /usr/local/bin/timestamp-line
RUN chmod +x /usr/local/bin/timestamp-line

RUN mkdir /var/log/marax

CMD ["/bin/sh", "-c", "cat /dev/ttyUSB0 | timestamp-line | tee -a /var/log/marax/temp.log"]
